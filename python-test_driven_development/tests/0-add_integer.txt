import doctest
doctest.testfile("0-add_integer.txt")

    try import function from module:
        >>> add_integer = __import__('0-add_integer').add_integer

    try testing regular addition:
        >>> add_integer(5, 5)
        10

    try method with negative number:
        >>> add_integer(10, -5)
        5

    try method without second argument:
        >>> add_integer(2)
        100

    try method with float:
        >>> add_integer(10, 5.5)
        15

    try method with string instead of int:
        >>> add_integer(5, "test")
        Traceback (most recent call last):
        TypeError: b must be an integer
